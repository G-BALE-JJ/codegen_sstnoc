# 协作工作流

## 基本原则

- 规划写在 `codegen_sstnoc`。
- 实现改动写在 `tilelang`。
- 任何重要设计变化都要写 ADR。
- 每次会话结束前更新 `progress.md`。

## 推荐顺序

1. 先看 `task_plan.md`。
2. 再看 `findings.md`。
3. 需要更深入的设计时查看 `docs/adr/`。
4. 在 `tilelang` 中修改源码，遵循 `tilelang/AGENTS.md`。
5. 回来更新 `progress.md`。
